# ARISE FINANCE — Phase 1B: integrate tested financial core into index.html

Read `docs/ARISE_SPEC.md`, `docs/ARISE_AUDIT.md`, `docs/ARISE_CODE_QA.md`, `financial-core.js`, and `tests/financial-core.test.js` from current branch/main before editing.

Goal: integrate the already-merged `financial-core.js` into the existing `index.html` safely, without changing product scope, auth, goals, Supabase, sync, currencies, or visual direction beyond the UI needed to expose correct financial state.

Important: the merged module publishes `window.ARISE_FINANCE_CORE`. Use that exact API; do not rename/rewrite the module unless a real defect is found and separately justified.

Required integration:
1. Load `financial-core.js` before the main inline application script.
2. Replace/route the legacy income planning and plan validation paths through `ARISE_FINANCE_CORE.planIncome` and `ARISE_FINANCE_CORE.validatePlan`.
3. Route month-level financial statistics through `ARISE_FINANCE_CORE.monthStats` (using a thin compatibility adapter only where current UI expects legacy field names). Do not duplicate new financial calculations in UI code.
4. Ensure `createIncomeTransaction` persists explicit `remainder`; historical income without remainder must remain readable via the core fallback.
5. Income proposal/editor must visibly show system remainder, allow a positive remainder, recalculate remainder after manual category/reserve edits, and block only over-allocation. No fake/category allocation may be created for remainder.
6. Remove all system behavior keyed on category display name `Свободные деньги`. The starter category with that name remains an ordinary editable category.
7. Explicit-category expense accounting must reduce that category by ID. Uncategorized expenses consume system free remainder; excess must surface as uncontrolled/untracked amount in derived stats/UX.
8. Category allocation modes shown to users are only fixed and percentage. Normalize legacy `remainder` category type safely to a supported ordinary type without data loss/crash. Do not reserve any category name.
9. Percent UI must accept every integer 1..100; presets are optional convenience only.
10. Starter categories for newly created finance profiles must match canon: `Обязательные расходы`, `Семья`, `Свободные деньги`; reserve separate. Do not modify existing user categories merely because they differ.
11. Reserve limit wording/behavior must be monthly/cumulative because the core now applies month-to-date limits.
12. Preserve current premium dark visual language, blur/glass, spacing, transitions, mobile layout. Any new remainder/uncontrolled state must look native to ARISE.
13. Do not introduce dead buttons or regress existing navigation/modals.

Tests/verification:
- `node --test tests/financial-core.test.js`
- `node --check financial-core.js`
- extract/check the inline JS from index.html with Node syntax check
- `git diff --check`
- inspect the final diff for magic-name calculations and duplicated month/income financial math
- manually trace Home, Income modal/proposal/save, Expenses preview/save/list, History, Analytics, Settings categories/reserve against the shared core

Because this Codex environment has previously failed to push commits, do NOT claim remote completion unless the GitHub PR diff actually changes. If push is unavailable, paste the COMPLETE unified diff for `index.html` (and any necessary test/module changes) directly into the PR conversation against the current PR base. Do not only summarize.

Remove this task file from the completed implementation.