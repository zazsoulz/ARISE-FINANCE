# ARISE FINANCE — P0 security: remove plaintext password persistence

Read `docs/ARISE_SPEC.md` and `docs/ARISE_CODE_QA.md` first.

Scope: remove plaintext password persistence from the current local prototype without implementing full Supabase Auth yet and without touching financial-core logic.

Required behavior:
1. `defaultState()` / account defaults must not include a stored password field.
2. `registerAccount()` may validate the password field for local prototype registration UX, but must never assign/save the raw password into application state.
3. `normalizeState()` / load migration must delete/ignore any historical `account.password` value so an existing stored plaintext password is removed on the next successful save.
4. `exportData()` must never include password/credential fields, even if malformed/legacy state somehow still contains them.
5. Do not put replacement password hashes/tokens in localStorage. This is only a temporary local prototype gate until Supabase Auth lands.
6. Preserve current registration flow and account/profile data. Do not introduce fake login/security claims.
7. Add a small repeatable Node/static regression check if practical, or at minimum verify extracted inline JS syntax and grep/assert that no code path persists `account.password`.
8. Keep user-facing text honest: local prototype registration is not production authentication.

Verification:
- inline JS syntax check
- `git diff --check`
- search for `account.password`, `password:` and exported credential fields
- ensure existing registration still transitions into app state without persisting the raw password
- inspect final diff for unrelated changes

Because Codex branch push has been unreliable, do not claim completion unless PR diff changes. If push is unavailable, paste the COMPLETE unified diff against this PR base directly into the PR conversation. Remove this task file when implementation is complete.
