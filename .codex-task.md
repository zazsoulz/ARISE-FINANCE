ARISE FINANCE task: fix the free-money core in index.html only. Do not redesign unrelated UI.

Required behavior:
1. User categories are arbitrary user-defined categories. The label "Свободные деньги" must NOT be treated as a reserved/system category anywhere.
2. Reserve/financial cushion is a separate system layer controlled by profile.settings.reserve.
3. After category allocations and reserve, any unallocated income is the system free remainder. It must be stored separately on the income transaction as `remainder`, not inserted into `allocations` as a fake category.
4. Spending with no selected category reduces system free money. Spending with an explicit category reduces that category's balance/statistics instead.
5. The income editor must allow a positive unallocated remainder. It should present that remainder as free money and only block saving when allocations + reserve exceed income.
6. Preserve existing data as safely as possible: historical transactions may not have `remainder`; use a sensible backwards-compatible fallback rather than breaking old data.
7. Do not change account/auth/profile architecture in this task.
8. Do not remove or rename unrelated categories. Do not make "Свободные деньги" special by name.

Inspect and update the connected logic together, especially: calculateIncomePlan, validatePlan, createIncomeTransaction, monthStats/currentFreeMoney, updateIncomePlanUI, saveIncomeFromModal, and any nearby code required for consistency.

Before finishing, check the embedded JavaScript in index.html for syntax errors and review the resulting diff for unintended changes. Commit only the necessary code changes to this PR branch. Remove this .codex-task.md task file before finishing.